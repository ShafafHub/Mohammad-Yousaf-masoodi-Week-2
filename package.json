{
  "name": "products-service",
  "version": "0.1.0",
  "private": true,
  "description": "Week 2 Products Service starter (pure Node HTTP)",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  }
}
